import { Button } from '@mui/material';

const PrimaryButton = () => {
  return (
    <Button variant="contained" color="secondary">
      Go to Checkout
    </Button>
  );
};

export { PrimaryButton };
